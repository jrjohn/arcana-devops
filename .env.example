# Jenkins
JENKINS_ADMIN_PASSWORD=admin
DOCKER_GID=999
TZ=Asia/Taipei

# Docker Registry
DOCKER_REGISTRY=localhost:5000

# Projects directory — MUST be an absolute host path
# This is where your project repos are cloned. Pipelines using 'docker compose run'
# require an absolute path so the Docker daemon can resolve volume mounts.
# Example: /home/user/devops-projects
PROJECTS_DIR=/path/to/your/projects

# Jenkins SSH Agents (optional — remove if not using remote agents)
# Place SSH private keys in jenkins/secrets/:
#   jenkins/secrets/MACMINI_SSH_KEY  — for macOS/iOS agent
#   jenkins/secrets/WINDOWS_SSH_KEY  — for Windows agent
MACMINI_HOST=                   # e.g. 10.0.0.10
MACMINI_USER=                   # e.g. jenkins
MACMINI_REMOTE_FS=/Users/jenkins/agent
WINDOWS_HOST=                   # e.g. 10.0.0.20
WINDOWS_USER=                   # e.g. jenkins
WINDOWS_REMOTE_FS=C:\jenkins-agent

# SonarQube DB
SONAR_DB_USER=sonarqube
SONAR_DB_PASSWORD=sonarqube

# Monitoring (used with docker-compose.monitoring.yml)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
PROMETHEUS_PORT=9090            # Change if 9090 is in use (e.g. Cockpit)
SONARQUBE_TOKEN=                # Create in SonarQube > My Account > Security > Generate Token
