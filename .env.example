# Jenkins
JENKINS_ADMIN_PASSWORD=admin
DOCKER_GID=999
TZ=Asia/Taipei
JENKINS_URL=https://arcana.boo/jenkins/
JENKINS_OPTS=--prefix=/jenkins

# Docker Registry
DOCKER_REGISTRY=localhost:5000

# GitHub PAT for SCM polling
GITHUB_TOKEN=

# Projects directory — MUST be an absolute host path
# This is where your project repos are cloned. Pipelines using 'docker compose run'
# require an absolute path so the Docker daemon can resolve volume mounts.
PROJECTS_DIR=/data/projects

# Jenkins JNLP Agents (Mac Mini + Windows connect outbound via WebSocket)
MACMINI_REMOTE_FS=/Users/jenkins/agent
WINDOWS_REMOTE_FS=C:\jenkins-agent

# SonarQube DB
SONAR_DB_USER=sonarqube
SONAR_DB_PASSWORD=sonarqube

# Monitoring (used with docker-compose.monitoring.yml)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
PROMETHEUS_PORT=9090            # Change if 9090 is in use (e.g. Cockpit)
SONARQUBE_TOKEN=                # Used by both monitoring exporter AND Jenkins analysis
SONAR_HOST_URL=http://sonarqube:9000/sonarqube
